# Secrets — never commit
.env
.env.*
!.env.example
*.key
*.pem

# Third-party clone (tutorial reference, not our work)
reference/

# Build artifacts
node_modules/
dist/
out/
cache/
broadcast/
artifacts/
.DS_Store

# Foundry dependencies (forge install). Anchor this at the repository root: an
# unanchored `lib/` also ignores our Solidity sources under `src/lib/`.
/lib/*
!/lib/forge-std
!src/lib/
!src/lib/**
!web/lib/
!web/lib/**

# Worker runtime state
state/

# Raw sanctions lists (57 MB) — downloaded by aml/fetch-lists.sh
data/raw/
state/
.vercel

# deck render cache
docs/deck/.DS_Store

# IDE state
.idea/

# Internal working notes — local only
/REVIEW.md
/TICKET.md
/TICKETS.md
/internal/
/ENHANCE.md
/docs/submission/dorahacks-form-answers.md
/docs/submission/team-information.md
/밸리데이터_Deck_2026.pdf
.env*.local
