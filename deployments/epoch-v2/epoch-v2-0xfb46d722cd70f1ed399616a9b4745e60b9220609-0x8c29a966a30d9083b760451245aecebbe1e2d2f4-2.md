# Epoch 2 — measured README snippets

Generated by `script/publish-epoch.ts` (checks 2026-09-14T00:41:37.907Z). Source publication facts can precede this invocation. Only propagation explicitly measured in this invocation is included. These RPC observations are not independent deployment or submission approval.

## Section 8 — replaces the bullet "Epoch rosters (Mode B) are built but not published on chain."

- **Epoch rosters (Mode B) are live: epoch 2 is published on chain.** The active set of 4 marks is one sorted-key Merkle root, `0xda906e635e6f61cab7c00984c4c701bf00ff0f24951e877bdcde1570d591250c`, published through `ComplianceSource` on Sepolia (`0x6e169b4b25d842e9cdbc10b5d237fa04c1148352b17f9273853e548883f3ae64`) and accepted by `ProofmarkASC` on CC3. Against that root the registry answers `verifyWithRoster` true under policy 2 and false under policy 1 for the same mark, and `proveNotInRoster` true for an address never issued to. The same adjacency proof is how a future subject removed from a roster is proven absent. The roster carries `validUntil 1789428960` (2026-09-14T23:36:00Z, a demo parameter; production cadence would be daily), after which `ASC.isRosterFresh()` is false and `verifyWithRoster` fails closed for every subject. Marks issued before the epoch keep `origin = Direct`; the roster is the set, not a rewrite of their provenance.

## Section 6 — one row for the cross-chain propagation table

_Not written: this run did not measure propagation (no publish transaction in it)._

